name: Python CI

on:
  push:
    branches: [main]
    paths: ['packages/python/**']
  pull_request:
    branches: [main]
    paths: ['packages/python/**']

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ['3.9', '3.10', '3.11', '3.12']

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e packages/python[dev]

      - name: Run tests
        run: |
          cd packages/python
          python -m pytest tests/ -v

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        # Pinned: an unpinned `ruff` picks up whatever new default rules the
        # latest release ships with, so this step can start failing on
        # previously-clean, unchanged code the moment a new ruff version
        # comes out - unrelated to anything in the PR being checked.
        run: pip install ruff==0.15.13

      - name: Lint
        run: ruff check packages/python/src/
